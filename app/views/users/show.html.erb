<p><%= @some_text %></p>

<p>
  <strong>First name:</strong>
  <%= @user.first_name %>

  <strong>Last name:</strong>
  <%= @user.last_name %>

  <strong>Email:</strong>
  <%= @user.email %>

  <strong><%= @user.first_name %>'s Boards</strong>
  <% @boards.each do |item| %>
  	<%= item.name %>
  <% end %>
</p>

<div id="navbar2">
  <table>
    <td><%= link_to 'Edit', edit_user_path(@user) %></td>
    <td><%= link_to 'Back', users_path %></td>
  </table>
</div>
